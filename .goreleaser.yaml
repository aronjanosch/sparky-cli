version: 2

project_name: sparky

before:
  hooks:
    - go mod tidy

builds:
  - main: .
    binary: sparky
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

brews:
  - name: sparky-cli
    repository:
      owner: aronjanosch
      name: homebrew-tap
      token: "{{ .Env.GITHUB_TOKEN }}"
    directory: Formula
    homepage: https://github.com/aronjanosch/sparky-cli
    description: CLI for SparkyFitness — log food, exercise, and biometrics from your terminal
    license: MIT
    install: bin.install "sparky"
    test: |
      system "#{bin}/sparky", "--version"

release:
  github:
    owner: aronjanosch
    name: sparky-cli
